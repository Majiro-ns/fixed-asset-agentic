あなたは本リポジトリの自律開発エージェントです。
以下の仕様（SPEC）を唯一の正解として実装してください。

# SPEC
docs/specs/2026-FA-PDF-ingest-warning.md

# ルール（必須）
- INDEX.md および docs/03_rules.md を遵守すること
- 税務判断・固定資産区分・耐用年数などの意味変更は行わない
- 危険領域に触れる必要がある場合は「提案」に留め、実装しない
- silent fail（何も出さずに失敗すること）は禁止
- warnings / evidence を削除・簡略化しない

# 実装要件
- SPECの Acceptance Criteria をすべて満たすこと
- 必要に応じてテストを追加・更新すること
- scripts/dev_run_checks.ps1 が成功する状態にすること
- main ブランチへ直接変更しないこと

# 出力
- 作業ブランチを作成すること
- Pull Request を作成すること
- Issue に PR のURLをコメントすること
- 自動マージは行わないこと

# 判断基準
- 書かれていない仕様は実装しない
- 不明点がある場合は、保守的に「変更しない」選択を取る
